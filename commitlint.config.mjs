// Commitlint config — enforces conventional commit format
// Valid prefixes: feat, fix, chore, docs, style, refactor, test, ci, perf
export default {
  extends: ['@commitlint/config-conventional'],
};
